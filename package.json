{
    "name": "minha-app-docker",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "lint": "eslint ."
    },
    "dependencies": {
        "express": "^4.19.2"
    },
    "devDependencies": {
        "eslint": "^10.10.0"
    }
}
